# tools/dice — the dice roller (stub)

Future conversation. A physical-play companion: d4/d6/d8/d10/d12/d20 rolls with the
table's own house feel, plus a PCG32 mode that follows CONTRACTS.md §3 exactly so a
digital table can reproduce an engine run die-for-die.

Contract surfaces: the RNG spec (§3) and the Decider decision-record format (§4) —
a roll can be exported as a decision record for the helper tool.
