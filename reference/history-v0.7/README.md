# history-v0.7 — retired fixtures of the v0.7 lineage

`sample_log_seed42.txt` is the v0.7 lineage's output for `--seed 42 --eras 20`
(LF line endings), generated by the v0.7 twin. It was the byte-identity golden
until v0.8 promoted the chapter 11 fields experiment into canon, which opened a
new lineage.

The live golden is now `../sample_log_seed42_v08.txt`, generated by
`../sim_v08.py`. Folders here are named for the **lineage** whose worlds they
describe, not the twin revision that produced them.
