# history-v0.4 — retired fixtures of the founding lineage

`sample_log_seed42.txt` is the v0.4 oracle's output for `--seed 42 --eras 20`
(LF line endings), generated by `../sim.py` — the frozen founding document. It
was the byte-identity golden until the v0.5 increment (the depth erratum and the
rename ledger) opened a new lineage; the live golden is now `../sample_log_seed42.txt`,
generated by `../sim_v05.py`.
