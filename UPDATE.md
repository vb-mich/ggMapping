# Update package: v0.7

Repo-shaped: every file in this package lands at its own path from the repo root.

* docs/0-Jerrymapping-the-game.md: REPLACES the handbook. Chapter 6 gains "The city
  lives" after step 5 and note 3 (Add Panel is the working panel).
* docs/FORK_NOTES.md: REPLACES the notes (new v0.7 section).
* reference/sim_v07.py: the new living twin. Per the standing ruling, delete the
  superseded reference/sim_v06.py; reference/sim.py (frozen v0.4) is untouched.
* reference/sample_log_seed42_v07.txt: the new lineage's seed-42 canon log, for the
  browser smoke golden after regeneration.

This is a RULES change: a new oracle lineage. The full matrix regenerates.
